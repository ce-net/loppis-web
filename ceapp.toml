[app]
name = "loppis-web"
version = "0.1.0"
summary = "loppis marketplace web frontend (typed mesh consumer; publish via ce-publish as a static bundle)"
runtime = "script"

[script]
interpreter = "sh"
entry = "run.sh"

[daemon]
enabled = true
